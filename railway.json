{
  "deploy": {
    "startCommand": "python patch_batter_upside_render.py && streamlit run app.py --server.address=0.0.0.0 --server.port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
